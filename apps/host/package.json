{
  "name": "@cf-mfe/host",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5173 --strictPort",
    "build": "tsc -b && vite build",
    "preview": "vite preview --port 5173 --strictPort",
    "//deploy:env": "Host uses @cloudflare/vite-plugin, which selects the wrangler env at BUILD time via CLOUDFLARE_ENV (it strips env blocks from its generated config), so `wrangler deploy --env` does NOT work here. VITE_APP_VERSION sets the wrangler --tag AND is baked into the build (import.meta.env.VITE_APP_VERSION).",
    "deploy:qa": "CLOUDFLARE_ENV=qa pnpm build && wrangler deploy --tag \"${VITE_APP_VERSION:?set VITE_APP_VERSION (e.g. VITE_APP_VERSION=v1.2.3)}\"",
    "deploy:prod": "CLOUDFLARE_ENV=prod pnpm build && wrangler deploy --tag \"${VITE_APP_VERSION:?set VITE_APP_VERSION (e.g. VITE_APP_VERSION=v1.2.3)}\"",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@module-federation/runtime": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "catalog:",
    "@module-federation/vite": "catalog:",
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@vitejs/plugin-react": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "wrangler": "catalog:"
  }
}
