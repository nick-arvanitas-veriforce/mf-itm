@alcumus:registry=https://npm.pkg.github.com
# The token is read from the environment — NEVER paste a literal one here, this
# file is committed. Locally it comes from .env (gitignored); on Render set
# NPM_TOKEN in the dashboard for each service that runs `pnpm install`.
//npm.pkg.github.com/:_authToken=${NPM_TOKEN}
