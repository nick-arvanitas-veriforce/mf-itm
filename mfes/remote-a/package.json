{
  "name": "@cf-mfe/remote-a",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5174 --strictPort",
    "build": "tsc -b && vite build",
    "preview": "vite preview --port 5174 --strictPort",
    "deploy:qa": "npm run build && wrangler deploy --env qa --tag \"${VITE_APP_VERSION:?set VITE_APP_VERSION (e.g. VITE_APP_VERSION=v1.2.3)}\"",
    "deploy:prod": "npm run build && wrangler deploy --env prod --tag \"${VITE_APP_VERSION:?set VITE_APP_VERSION (e.g. VITE_APP_VERSION=v1.2.3)}\""
  },
  "dependencies": {
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  },
  "devDependencies": {
    "@module-federation/vite": "^1.16.8",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "typescript": "~6.0.2",
    "vite": "^8.0.12",
    "wrangler": "^4.100.0"
  }
}
